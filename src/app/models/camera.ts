export interface Camera {
  cameraID: number;
  cameraName: string;
  brand: string;
  price: number;
  img: string;
  quantity: number;
  categoryName: string;
  description: string;
  bestSeller: string;
}